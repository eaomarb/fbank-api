spring:
  data:
    web:
      pageable:
        max-page-size: 25
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
jwt:
  secret: ${JWT_SECRET}